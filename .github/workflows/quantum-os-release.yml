name: Release Quantum OS

on:
  push:
    tags:
      - "quantum-os-v*"

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push immutable image
        id: image
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: quantum_os
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/kkopan/quantum-os:${{ github.ref_name }}

      - name: Create signed update manifest
        env:
          IMAGE_DIGEST: ${{ steps.image.outputs.digest }}
          SIGNING_KEY: ${{ secrets.QUANTUM_OS_UPDATE_SIGNING_KEY }}
        run: |
          python -m pip install --disable-pip-version-check pynacl==1.6.2
          python - <<'PY'
          import base64
          import json
          import os
          from datetime import UTC, datetime
          from pathlib import Path
          from nacl.signing import SigningKey

          payload = {
              "schema_version": 1,
              "release": os.environ["GITHUB_REF_NAME"],
              "repository": os.environ["GITHUB_REPOSITORY"],
              "image": f"ghcr.io/{os.environ['GITHUB_REPOSITORY_OWNER']}/kkopan/quantum-os",
              "digest": os.environ["IMAGE_DIGEST"],
              "published_at": datetime.now(UTC).isoformat(),
          }
          manifest = json.dumps(
              payload, separators=(",", ":"), sort_keys=True
          ).encode()
          key = SigningKey(base64.b64decode(os.environ["SIGNING_KEY"]))
          Path("quantum-os-update.json").write_bytes(manifest)
          Path("quantum-os-update.sig").write_bytes(
              base64.b64encode(key.sign(manifest).signature)
          )
          PY

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            quantum-os-update.json quantum-os-update.sig \
            --generate-notes --verify-tag
