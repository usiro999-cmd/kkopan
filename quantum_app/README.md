# Quantum Circuit Lab

Python標準ライブラリだけで動く、状態ベクトル方式の量子回路シミュレーターです。

```bash
cd quantum_app
python3 app.py
```

ブラウザで <http://127.0.0.1:8000> を開いてください。

H、X、Y、Z、S、T、RY、CXゲートに対応し、最大8量子ビットまで計算できます。
テストは `python3 -m unittest -v` で実行できます。
